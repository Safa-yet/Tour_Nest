import React from 'react';

const loading = () => {
    return (
        <div className="flex items-center justify-center h-screen bg-[#f7fbeb] text-black text-5xl">
            Loading
        </div>
    );
};

export default loading;